/**
 * tab 기능 구현
 *
 * 각 tab 버튼을 클릭하면 해당 tab을 표시해준다.
 * 각 tab 버튼을 클릭하면 각 관련된 컨텐츠를 표시한다. (index로 구분)
 */

const classNames = {
  TAB: "tab",
  CONTENT: "content"
};

const createTab = (el: HTMLElement) => {
  // element 캐싱 (tab, content)

  // tab 버튼 클릭 이벤트

  // content 노출 핸들러
}